# ============================================================================
# KMS Frontend Configuration
# ============================================================================
# Copy this file to .env and update values as needed
#
# IMPORTANT:
# - All frontend environment variables MUST start with VITE_
# - Never expose sensitive credentials (passwords, API keys) in frontend
# - .env files should be added to .gitignore
# ============================================================================

# ==================== Application ====================
# Application environment: development | staging | production
VITE_APP_ENV=development

# ==================== API Configuration ====================
# Backend API base URL
VITE_API_URL=http://localhost:8000/api/v1

# ==================== Authentication ====================
# Google OAuth Client ID (get from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# SSO Provider URL (if using corporate SSO)
VITE_SSO_PROVIDER_URL=

# ==================== Service Endpoints ====================
# These are for status display and health checks only
# Actual API calls go through the backend

# Neo4j Browser URL (for admin links)
VITE_NEO4J_BROWSER_URL=http://localhost:7474

# LLM Service URLs (for status monitoring)
VITE_NEMOTRON_LLM_URL=http://localhost:12800
VITE_NEMO_EMBEDDING_URL=http://localhost:12801
VITE_MISTRAL_CODER_URL=http://localhost:12802

# ==================== Feature Flags ====================
# Enable/disable features
VITE_ENABLE_MINDMAP=true
VITE_ENABLE_KNOWLEDGE_GRAPH=true
VITE_ENABLE_VISION=true

# ==================== UI Configuration ====================
# Default theme: dark | light
VITE_DEFAULT_THEME=dark

# Default language: en | ko | ja
VITE_DEFAULT_LANGUAGE=en

# ============================================================================
# Environment-Specific Overrides
# ============================================================================
# Create additional files for different environments:
#   .env.development  - Development settings
#   .env.staging      - Staging settings
#   .env.production   - Production settings
#   .env.local        - Local overrides (highest priority, git-ignored)
#
# Priority: .env.local > .env.[mode] > .env
# ============================================================================
